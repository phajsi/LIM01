import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  replaybutton: {
    border: '1px solid #F56F4C',
    borderRadius: 5,
    justifyContent: 'flex-end',
  },
});
export default useStyles;
