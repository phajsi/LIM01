import React from 'react';
import { Grid } from '@material-ui/core';

const OverviewPage = () => {
  const text = 'Dette er en fillertext';

  return (
    <Grid>
      <p>{text}</p>
    </Grid>
  );
};

export default OverviewPage;
